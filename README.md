# NaturalSQL---Typescript
A lightweight library to convert your SQL database schema into a vector database, providing LLMs with the necessary context to generate accurate SQL queries from natural language.
